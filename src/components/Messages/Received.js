import React from 'react';
import Tabs from './Tabs';

export default function Sent() {
    return (
        <section>
            <Tabs />
            <section>
                You have no proposals received currently.
            </section>
        </section>
    )
}
