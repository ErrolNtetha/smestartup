describe('Home', () => {
    beforeEach(() => {
        cy.visit('/')
    })

    it('it renders the login button with "Login"', () => {
        cy.visit('/');
    });

    it('shows the link button', () => {
        cy.visit(/)
    })
});
